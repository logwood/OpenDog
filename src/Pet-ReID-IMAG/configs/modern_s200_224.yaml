_BASE_: modern_s101_224.yaml

MODEL:
  BACKBONE:
    DEPTH: 200x
    PRETRAIN_PATH: models/pretrained/resnest200-75117900.pth

SOLVER:
  IMS_PER_BATCH: 28
  # Scale warmup by 64/28 for sample exposure, but retain the author's
  # 1000-update backbone-freeze threshold just like the S101 branches.
  WARMUP_ITERS: 914
  FREEZE_ITERS: 1000

OUTPUT_DIR: artifacts/runs/legacy/modern_s200_224
