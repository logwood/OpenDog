# "/" means that the browser uses this frontend's /v1 same-origin proxy.
# This is the recommended mode for Android/LAN access.
NEXT_PUBLIC_PET_REID_API_BASE_URL=/
PET_REID_GATEWAY_PROXY_TARGET=http://127.0.0.1:8080
PET_REID_FRONTEND_HOST=127.0.0.1
PET_REID_FRONTEND_PORT=3000
NEXT_PUBLIC_SITE_ORIGIN=http://localhost:3000
